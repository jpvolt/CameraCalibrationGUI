#pragma once
/*
#include <opencv2/opencv.hpp>
#include <string>
#include <vector>
using namespace cv;
class FileHandler{
    private:
    std::vector<Mat&> matstofile;
    std::vector<std::string> matsnames;
    public:
    FileHandler();
    void addMat(Mat& mat, std::string MatName);
    void Save(std::string filePath);
};
*/