#include "fileHandler.hpp"
/*
FileHandler::FileHandler(){
    
}
void FileHandler::addMat(Mat& mat, std::string MatName){
    matstofile.push_back(mat);
    matsnames.push_back(MatName);
}
void FileHandler::Save(std::string filePath){
    FileStorage fileOut(filePath, FileStorage::WRITE);
    for(int i = 0; i< matstofile.size(); i++){
        fileOut << matsnames[i] << matstofile[i];
    }
    fileOut.release();
}
*/